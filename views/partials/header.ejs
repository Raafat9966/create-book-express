<nav class="navbar navbar-dark bg-dark">
	<div class="container-fluid row">
		<div class="container d-flex m-0 col-9">
			<a class="navbar-brand" href="/books">Books</a>
			<ul class="navbar-nav">
				<li class="nav-item">
					<a class="nav-link" href="/books/add-book">Add Book</a>
				</li>
			</ul>
		</div>
		<% if(page === "books"){ %>
		<form class="d-flex col" action="/find" method="GET">
			<input
				class="form-control me-2"
				type="search"
				placeholder="Search"
				aria-label="Search"
				name="search"
			/>
			<button class="btn btn-outline-success" type="submit">
				Search
			</button>
		</form>
		<% } else if(page === "home"){ %>
		<a class="navbar-brand" href="/user/register">Register</a>
		<% } else if(page === "register"){ %>
		<a class="navbar-brand" href="/">login</a>
		<% } %>
	</div>
</nav>
